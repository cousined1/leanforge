{
  "name": "leanforge-root",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd keyword-trend-api && npm install && npm run build",
    "start": "cd keyword-trend-api && node dist/index.js",
    "postinstall": "cd keyword-trend-api && npm install"
  },
  "engines": {
    "node": ">=20"
  }
}
